import DS from 'ember-data';
/*
this is normally a RESTAdapter when not using dummy data also FixtureAdapter
is getting moved into an unsupported addon. Not sure what the replacement is
*/

export default DS.FixtureAdapter.extend({});
